addons:
  apt:
    packages:
      - fonts-sil-doulos
      - lmodern
      - pgf
      - texlive-base
      - texlive-science
      - texlive-fonts-extra
      - texlive-fonts-recommended
      - texlive-lang-greek
      - texlive-lang-czechslovak
      - texlive-latex-base
      - texlive-latex-extra
      - texlive-latex-recommended
      - texlive-math-extra
      - texlive-xetex 
# for tests
      - latex-beamer
      - poppler-utils
      - python-matplotlib
      - python-numpy


before_install:

install:

before_script:

script:
  - make test

after_failure:
  - echo "Test failed, see respective PNGs"
